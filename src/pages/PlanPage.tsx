// Sprint 4: plan creation wizard, calendar/week view, workout status tracking,
// archive-and-restart, full wipe (with confirm + export prompt).
export function PlanPage() {
  return (
    <section>
      <h2 className="text-xl font-semibold">Training Plan</h2>
      <p className="mt-2 text-muted-foreground">
        Generate a multi-week plan (coming in Sprint 4).
      </p>
    </section>
  );
}
