// Sprint 3: one global, plan-aware coach thread (no per-run threads).
// Streaming responses, run summary auto-injected after upload,
// "chat needs network" banner when offline.
export function ChatPage() {
  return (
    <section>
      <h2 className="text-xl font-semibold">Coach</h2>
      <p className="mt-2 text-muted-foreground">
        Chat with your coach (coming in Sprint 3).
      </p>
    </section>
  );
}
